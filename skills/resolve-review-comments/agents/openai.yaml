interface:
  display_name: "Resolve Review Comments"
  short_description: "Handle review comments on GitLab, GitHub, and more"
  default_prompt: "Use $resolve-review-comments on these code reviews, wherever they are hosted: fix valid issues, explain each decision briefly, reply, resolve handled AI threads, and leave human threads open."
